// Empty stub for the `canvas` package required by pdfjs-dist on Node.
// In React Native we don't render PDFs to canvas — we only extract text —
// so a no-op is enough to satisfy the import resolver.
module.exports = {};

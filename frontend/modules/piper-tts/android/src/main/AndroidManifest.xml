<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    Empty application block now. The TTS playback foreground service +
    MediaSession that used to live here is replaced by react-native-track-player,
    which ships its own ExoPlayer-backed service and MediaSession on the
    app side (registered via index.js).
  -->
</manifest>
